// Service for managing quote operations
// You can write here some custom logic for your quote service

import quote from './crud';
export { QuoteSchema } from '@/src/types/schemas';

export default quote;
